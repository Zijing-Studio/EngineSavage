unit=GetUnit(id);
if(unit["pid"]==pid)
{
size=GetUnitCnt();
i=2;
while(i<size)
{
unit2=GetUnit(i);
if((unit2["status"]&2)==0)
{
if((unit2["pos"]-unit["pos"])["Length"]<=2&&unit2["pid"]==unit["pid"])
{
ChangeHealth(i,id,1);
}
}
i=i+1;
}
}